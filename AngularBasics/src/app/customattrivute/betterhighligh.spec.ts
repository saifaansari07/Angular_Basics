import { Betterhighligh } from './betterhighligh';

describe('Betterhighligh', () => {
  it('should create an instance', () => {
    const directive = new Betterhighligh();
    expect(directive).toBeTruthy();
  });
});
